# 消费管家产品方案

状态：当前实现基线  
当前版本：1.0.1（构建号 2）  
版本格式：用户界面只显示纯数字语义版本，不使用 `v` 前缀

## 1. 产品定义

消费管家是一款本地优先的个人消费记录 App。它把支付截图、通知文本和手动输入统一转换为可复核的消费记录，再通过明细、预算和周期报告帮助用户理解支出。

产品不追求覆盖完整财务会计，而是优先解决三个问题：

1. 少输入：截图或通知可以快速生成候选记录。
2. 不误记：自动识别必须先复核，重复记录必须可解释。
3. 看得懂：总览回答当前状态，报告负责周期复盘，两个页面不重复堆图表。

## 2. 核心原则

- 本地优先：账本、规则、报告和识别统计默认保存在本机。
- 先复核后入账：截图、分享扩展和文本解析只生成候选，不静默写入账本。
- 单入口单职责：总览看状态，明细查记录，导入处理候选，报告做复盘，我的承接设置与信任。
- 解释结果：用户看到识别、重复和失败原因，不需要理解 OCR 供应商或解析管线。
- 操作可恢复：删除、批量操作和低置信度结果必须有确认或后续恢复能力。
- 版本同步：每次版本迭代同时更新构建版本、我的页变化简介和本文档。

## 3. 目标用户与任务

目标用户是每天通过微信、支付宝、银行卡、外卖和电商产生多笔消费，愿意少量复核但不愿逐笔录入的人。

关键任务按优先级排序：

1. 导入一张支付截图并确认候选。
2. 在十秒内找到一笔历史消费。
3. 手动补记一笔无法自动识别的消费。
4. 判断本月预算是否接近或超过限制。
5. 查看一段周期内的变化、集中项和未整理记录。

## 4. 信息架构

```text
总览
  本月支出 / 主要分类 / 预算 / 最近记录 / 记一笔

明细
  搜索 / 月份 / 分类 / 场景 / 渠道 / 编辑 / 批量删除

导入
  待复核队列 / 当前批次 / 截图 / 文本 / 手动补录

报告
  周期切换 / 消费洞察 / 历史总结 / 报告刷新

我的
  本机账本 / 外观 / 类型管理 / 推送 / 隐私 / 版本变化
```

底部保持五个稳定 tab。分析不再单独占用入口；当前状态留在总览，周期分析只出现在报告。

## 5. 核心流程

### 5.1 截图导入

1. 用户从导入页、分享扩展或深链进入。
2. 选择支付渠道和截图。
3. 系统按可用能力识别文字，并尽量保留每条记录的商户标识。
4. 系统生成导入批次，标记待复核或疑似重复。
5. 用户确认、修改或忽略候选。
6. 确认后统一刷新明细、预算、报告和通知排程。

### 5.2 文本与手动录入

- 文本解析与截图共用批次、候选、去重和复核模型。
- 手动录入直接进入编辑器，保存后走统一写入副作用。
- 无法识别时必须提供文本粘贴和手动录入两条退路。

### 5.3 查询与复盘

- 明细支持按商户、分类、场景、渠道和备注搜索，并可叠加月份与快捷筛选。
- 总览只展示本月关键状态、预算和最近记录。
- 报告展示周期趋势、集中项、整理状态和历史总结，不复制总览的操作控件。

## 6. 当前版本需求

### P0 可靠性

- 深链 `awemebilling://review-import` 必须进入导入页并显示真实待复核队列。
- 临时成功或失败提示不得遮挡待复核候选。
- 同商户、同金额、同渠道和分钟级时间的重复记录必须拦截。
- 结构化识别结果如果包含无效交易、汇总金额或关闭订单，不得回退成伪消费记录。
- 接受候选、编辑和删除记录后，依赖数据必须一致刷新。

### P0 可用性

- 待复核内容在导入页位于新增导入之前。
- 明细必须支持原生搜索。
- 总览不提供图表样式配置，也不重复报告洞察。
- 报告必须有稳定主入口。
- 我的页显示当前版本和本版本变化，并可回看历史版本。

### P1 信任与数据安全

- 普通设置不展示识别供应商名称、密钥状态或工程术语。
- 用户能理解账本存放位置、图片何时可能使用在线识别、统计是否包含消费内容。
- 后续补齐删除撤销、导出与恢复。

## 7. 非目标

- 当前不做账号体系、云同步和多人账本。
- 当前不做资产负债、投资收益、发票或企业报销。
- 当前不开放 OCR 供应商选择给普通用户。
- 当前不新增只重复分类排名或渠道排名的分析页面。

## 8. 成功标准

- 分享或深链进入后一步可见待复核记录。
- 新用户 30 秒内可以完成一次导入或记账。
- 搜索常见商户时十秒内找到目标记录。
- 同一候选重复确认不会生成两笔记录。
- 核心解析、去重、周期和报告测试全部通过。
- 模拟器产物只包含原生 `arm64` 架构。
- 我的页版本号与构建设置一致，且不包含 `v` 前缀。

## 9. 版本规划

### 1.0.1 当前校准

- 总览收敛为本月状态、预算和最近记录。
- 导入页前置待复核队列。
- 明细增加搜索。
- 恢复报告主入口，移除未使用的场景分析页面。
- 我的页增加本版本变化和历史版本变化。
- 模拟器构建统一为原生 arm64，清理 C++ 和手工链接设置。

### 1.1 导入可信化

- 候选字段级置信度和快速修改。
- 导入历史与失败重试。
- 将写入后的报告与通知刷新完全收口到统一服务。
- 扩充真实脱敏文本和截图回归样本。

### 1.2 报告与规则

- 报告按周期唯一键 upsert，保留生成依据和版本。
- 报告详情回跳对应明细筛选。
- 用户修正分类后形成可管理规则。
- 删除撤销与 CSV 导出。

### 1.3 Beta 准备

- 首次使用引导、数据迁移、备份恢复。
- 权限和在线识别隐私说明收口。
- 50 条以上脱敏样本回归及发布清单自动检查。

## 10. 版本发布规则

每次按版本规划实施时必须同时完成：

1. 更新 App 与 Share Extension 的 `MARKETING_VERSION` 和构建号。
2. 更新我的页 `AppReleaseNotes`，文案使用用户语言。
3. 更新本文档的当前版本、当前需求和版本规划状态。
4. 执行 `SIMULATOR_ID=<id> scripts/quality_gate.sh`。
5. 确认 UI 中没有 `v1.2` 这类带前缀版本号。
